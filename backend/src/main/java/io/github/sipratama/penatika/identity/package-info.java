/**
 * Owns teacher identity, authentication, browser-session, and participant-access concepts.
 * Other business modules must depend on identity through explicit public module boundaries,
 * not identity internals.
 */
package io.github.sipratama.penatika.identity;
